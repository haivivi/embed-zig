# ESP32 build settings

load("//bazel/esp:defs.bzl", "esp_monitor")

package(default_visibility = ["//visibility:public"])

# Serial monitor (uses Go binary from //bazel/esp/tools/monitor)
esp_monitor(
    name = "monitor",
)
