# ESP32 build settings

load("//bazel/esp:defs.bzl", "esp_monitor")

package(default_visibility = ["//visibility:public"])

# Build scripts
filegroup(
    name = "scripts",
    srcs = glob(["scripts/*.sh"]),
)

# Serial monitor
esp_monitor(
    name = "monitor",
)
