# e2e: trait/log — std platform test (CI-mandatory)

load("//bazel/zig:defs.bzl", "zig_test")

package(default_visibility = ["//visibility:public"])

zig_test(
    name = "test",
    main = "test.zig",
    srcs = [
        "test.zig",
        "board.zig",
    ],
    deps = [
        "//e2e/trait/log:e2e_log",
    ],
    tags = ["e2e"],
)
