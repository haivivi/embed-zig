# e2e: trait/sync — cross-platform test logic

load("//bazel/zig:defs.bzl", "zig_library")

package(default_visibility = ["//visibility:public"])

zig_library(
    name = "e2e_sync",
    main = "app.zig",
    srcs = ["app.zig"],
    module_name = "e2e_sync",
    deps = [
        "//lib/trait",
        "//lib/pkg/async/channel",
        "//lib/pkg/async/waitgroup",
    ],
)
