# Opus Native Test — run on macOS to verify encode/decode
#
# Build: bazel build //examples/apps/opus_aec_test/native:test
# Run:   bazel run //examples/apps/opus_aec_test/native:test

load("//bazel/zig:defs.bzl", "zig_binary")

package(default_visibility = ["//visibility:public"])

zig_binary(
    name = "test",
    main = "main.zig",
    srcs = ["main.zig"],
    deps = [
        "//lib/pkg/audio",
        "//third_party/opus:opus_fixed",
    ],
    optimize = "ReleaseFast",
)
