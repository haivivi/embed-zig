"""UI Demo Raylib Simulator."""

load("//bazel/raysim:defs.bzl", "raysim_run")

raysim_run(
    name = "run",
    srcs = glob(
        ["**/*"],
        exclude = [".zig-cache/**", "zig-out/**"],
    ),
    project_dir = "examples/raysim/ui_demo",
)
