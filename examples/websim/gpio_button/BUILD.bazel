load("//bazel/websim:defs.bzl", "websim_app")

package(default_visibility = ["//visibility:public"])

websim_app(
    name = "gpio_button",
    main = "wasm_main.zig",
    srcs = [
        "wasm_main.zig",
        "platform.zig",
    ],
    deps = [
        "//lib/platform/websim",
        "//lib/hal",
    ],
)
