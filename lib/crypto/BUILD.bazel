# Crypto library - Pure Zig cryptographic implementations

load("//bazel/zig:defs.bzl", "zig_lib")

package(default_visibility = ["//visibility:public"])

zig_lib(
    name = "crypto",
    srcs = glob(["**/*"], exclude = ["certs/**"]),
    deps = ["//lib/trait"],
)
