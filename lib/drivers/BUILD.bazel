# Hardware drivers

package(default_visibility = ["//visibility:public"])

filegroup(
    name = "drivers_sources",
    srcs = glob(
        ["**/*.zig", "**/*.zon"],
        exclude = [".zig-cache/**", "zig-out/**"],
        allow_empty = True,
    ),
)
