load("//bazel/zig:defs.bzl", "zig_package", "zig_static_library")

package(default_visibility = ["//visibility:public"])

zig_package(
    name = "hal",
    deps = [
        "//lib/trait",
        "//lib/pkg/motion",
    ],
)

zig_static_library(
    name = "hal_a",
    lib = ":hal",
)
