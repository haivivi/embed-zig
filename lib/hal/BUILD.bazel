# HAL (Hardware Abstraction Layer)

package(default_visibility = ["//visibility:public"])

filegroup(
    name = "hal_sources",
    srcs = glob(
        ["**/*.zig", "**/*.zon"],
        allow_empty = True,
        exclude = [".zig-cache/**", "zig-out/**"],
    ),
)
