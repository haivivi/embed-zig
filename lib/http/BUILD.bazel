# HTTP library

package(default_visibility = ["//visibility:public"])

filegroup(
    name = "http_sources",
    srcs = glob(
        ["**/*.zig", "**/*.zon"],
        allow_empty = True,
        exclude = [".zig-cache/**", "zig-out/**"],
    ),
)
