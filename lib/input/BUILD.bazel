# Input library

package(default_visibility = ["//visibility:public"])

filegroup(
    name = "input_sources",
    srcs = glob(
        ["**/*.zig"],
        allow_empty = True,
        exclude = [".zig-cache/**", "zig-out/**"],
    ),
)
