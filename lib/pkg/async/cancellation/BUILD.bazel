load("//bazel/zig:defs.bzl", "zig_package")

package(default_visibility = ["//visibility:public"])

zig_package(
    name = "cancellation",
    module_name = "async/cancellation",
)

filegroup(name = "srcs", srcs = glob(["**/*"]))
