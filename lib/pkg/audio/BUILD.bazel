load("//bazel/zig:defs.bzl", "zig_package")

package(default_visibility = ["//visibility:public"])

# No test: opus.zig uses @cImport("opus.h") which needs system headers
zig_package(
    name = "audio",
    test = False,
)

filegroup(name = "srcs", srcs = glob(["**/*"]))
