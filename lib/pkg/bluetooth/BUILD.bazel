load("//bazel/zig:defs.bzl", "zig_lib")

package(default_visibility = ["//visibility:public"])

zig_lib(
    name = "bluetooth",
    srcs = glob(["**/*"]),
    deps = [
        "//lib/trait",
        "//lib/pkg/channel",
        "//lib/pkg/waitgroup",
        "//lib/pkg/cancellation",
        "//lib/platform/std",
    ],
)
