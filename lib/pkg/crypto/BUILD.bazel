load("//bazel/zig:defs.bzl", "zig_package")

package(default_visibility = ["//visibility:public"])

# No test: suite.zig references HkdfSha384 not available in Xtensa Zig fork
zig_package(
    name = "crypto",
    main = "src/suite.zig",
    deps = ["//lib/trait"],
    test = False,
)
