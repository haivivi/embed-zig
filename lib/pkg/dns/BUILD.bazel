load("//bazel/zig:defs.bzl", "zig_package")

package(default_visibility = ["//visibility:public"])

zig_package(
    name = "dns",
    deps = [
        "//lib/platform/std",
        "//lib/pkg/tls",
        "//lib/trait",
    ],
)
