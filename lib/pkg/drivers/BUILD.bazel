load("//bazel/zig:defs.bzl", "zig_package")

package(default_visibility = ["//visibility:public"])

# No test: qmi8658.zig has pre-existing API mismatch in test code
zig_package(
    name = "drivers",
    deps = [
        "//lib/trait",
        "//lib/pkg/math",
    ],
    test = False,
)
