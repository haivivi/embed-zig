load("//bazel/zig:defs.bzl", "zig_package")

package(default_visibility = ["//visibility:public"])

zig_package(
    name = "drivers",
    deps = [
        "//lib/trait",
        "//lib/pkg/math",
    ],
)
