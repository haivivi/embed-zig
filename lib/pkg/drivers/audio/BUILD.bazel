load("//bazel/zig:defs.bzl", "zig_package")

package(default_visibility = ["//visibility:public"])

zig_package(
    name = "audio",
    module_name = "audio_drivers",
    deps = [
        "//lib/trait",
    ],
)

filegroup(name = "srcs", srcs = glob(["**/*"]))
