load("//bazel/zig:defs.bzl", "zig_package")

package(default_visibility = ["//visibility:public"])

zig_package(
    name = "input",
    main = "src/adc_button.zig",
)

filegroup(name = "srcs", srcs = glob(["**/*"]))
