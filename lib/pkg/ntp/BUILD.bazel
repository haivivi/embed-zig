load("//bazel/zig:defs.bzl", "zig_package")

package(default_visibility = ["//visibility:public"])

zig_package(
    name = "ntp",
    deps = [
        "//lib/platform/std",
        "//lib/trait",
    ],
)
