# NTP library - Cross-platform NTP client

load("//bazel/zig:defs.bzl", "zig_lib")

package(default_visibility = ["//visibility:public"])

zig_lib(
    name = "ntp",
    srcs = glob(["**/*"]),
    deps = [
        "//lib/platform/std",
        "//lib/trait",
    ],
)
