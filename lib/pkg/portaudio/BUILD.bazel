load("//bazel/zig:defs.bzl", "zig_package")

package(default_visibility = ["//visibility:public"])

# No test: portaudio.zig uses @cImport("portaudio.h") which needs system headers
zig_package(
    name = "portaudio",
    test = False,
)

filegroup(name = "srcs", srcs = glob(["**/*"]))
