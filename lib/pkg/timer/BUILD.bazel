load("//bazel/zig:defs.bzl", "zig_package")

package(default_visibility = ["//visibility:public"])

zig_package(
    name = "timer",
    deps = [
        "//lib/trait",
        "//lib/hal",
    ],
    test_deps = ["//lib/platform/std"],
)

filegroup(name = "srcs", srcs = glob(["**/*"]))
