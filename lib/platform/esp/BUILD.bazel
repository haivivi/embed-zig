# ESP platform — file copying only (compiled via esp_zig_app + CMake, not Bazel-native zig)

package(default_visibility = ["//visibility:public"])

filegroup(
    name = "esp",
    srcs = glob(["**/*"], exclude = ["idf/**", "impl/**"]) + [
        "//lib/platform/esp/idf",
        "//lib/platform/esp/impl",
    ],
)
