# ESP trait + hal implementations — source files only
#
# Compiled as part of per-app esp zig_library (needs sdkconfig.h from idf).

package(default_visibility = ["//visibility:public"])

filegroup(
    name = "zig_srcs",
    srcs = glob(["src/**/*.zig"]),
)

# All files (backward compat)
filegroup(
    name = "impl",
    srcs = glob(
        ["**/*"],
        exclude = [".zig-cache/**", "zig-out/**"],
    ),
)
