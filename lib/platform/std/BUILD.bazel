load("//bazel/zig:defs.bzl", "zig_package")

package(default_visibility = ["//visibility:public"])

# No test: platform impl has compile errors without full platform context
zig_package(
    name = "std",
    module_name = "std_impl",
    deps = ["//lib/trait"],
    test = False,
)

filegroup(name = "srcs", srcs = glob(["**/*"]))
