# Raysim library - Raylib simulator bindings

load("//bazel/zig:defs.bzl", "zig_lib")

package(default_visibility = ["//visibility:public"])

zig_lib(
    name = "raysim",
    srcs = glob(["**/*"]),
)
