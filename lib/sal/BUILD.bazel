# SAL (System Abstraction Layer)

package(default_visibility = ["//visibility:public"])

filegroup(
    name = "sal_sources",
    srcs = glob(
        ["**/*.zig", "**/*.zon"],
        allow_empty = True,
    ),
)
