# Standard library abstraction

package(default_visibility = ["//visibility:public"])

filegroup(
    name = "std_sources",
    srcs = glob(
        ["**/*.zig", "**/*.zon"],
        allow_empty = True,
        exclude = [".zig-cache/**", "zig-out/**"],
    ),
)
